# 视频混剪智能体原型优化说明

## 优化概览

本次优化对视频混剪智能体进行了两项重要的功能增强：
1. 增加"视频生成数量"参数配置
2. 新增"标题配置"步骤（步骤从4步增加到5步）

---

## 一、视频生成数量参数

### 1.1 功能描述
在参数配置中新增"视频生成数量"输入框，允许用户自定义批量生成视频的数量。

### 1.2 位置
位于"视频标题"和"视频时长"之间（参数配置页面）

### 1.3 参数配置
- **默认值**: 3个
- **最小值**: 1个
- **最大值**: 100个
- **输入类型**: 数字输入框
- **提示文字**: "默认3个" + "最多100个"

### 1.4 UI 设计
```
视频标题
┌─────────────────────────────────────┐
│ 输入视频标题                        │
└─────────────────────────────────────┘

视频生成数量
┌──────────────────────┬──────────────┐
│ [数字输入框]         │ 最多100个    │
└──────────────────────┴──────────────┘

视频时长 (60秒)
━━━━━━━━━━━●━━━━━━━━━━━━━━━━━━━━━━━
```

---

## 二、标题配置步骤

### 2.1 功能描述
新增独立的"标题配置"步骤，支持主副标题的详细样式配置。

### 2.2 步骤变化
- **原来**: 步骤 1/4（模板选择 → 参数配置 → 字幕配置 → 音色配置）
- **现在**: 步骤 1/5（模板选择 → 参数配置 → **标题配置** → 字幕配置 → 音色配置）

### 2.3 配置结构

#### 2.3.1 全局配置（主副标题样式设置）
高亮蓝色背景框，包含3个全局参数：

| 参数名称 | 选项 | 默认值 |
|---------|------|--------|
| 标题位置 | 顶部/中间/底部/自定义 | 顶部 |
| 主副标题间距 | 数字输入(px) | 11px |
| 对齐方式 | 左对齐/居中/右对齐 | 居中 |

#### 2.3.2 主标题配置
- **开关**: 启用/禁用（默认启用）
- **标签颜色**: 蓝色

**配置参数**:

| 参数名称 | 类型 | 默认值 | 说明 |
|---------|------|--------|------|
| 标题文本 | 文本输入 | - | 输入主标题内容 |
| 字间距 | 数字输入(px) | -50px | 字符间距 |
| 字体大小 | 数字输入(px) | 64px | 字号大小 |
| 颜色 | 颜色选择器 + 文本输入 | #FFFFFF | 文字颜色 |
| 字体 | 下拉选择 | 思源黑体Heavy | 字体样式 |

**字体选项**:
- 思源黑体Heavy（默认）
- PingFang SC
- Microsoft YaHei
- 黑体
- 宋体

#### 2.3.3 副标题配置
- **开关**: 启用/禁用（默认禁用）
- **标签颜色**: 绿色

**配置参数**: 与主标题配置完全相同

| 参数名称 | 类型 | 默认值 | 说明 |
|---------|------|--------|------|
| 标题文本 | 文本输入 | - | 输入副标题内容 |
| 字间距 | 数字输入(px) | -50px | 字符间距 |
| 字体大小 | 数字输入(px) | 64px | 字号大小 |
| 颜色 | 颜色选择器 + 文本输入 | #FFFFFF | 文字颜色 |
| 字体 | 下拉选择 | 思源黑体Heavy | 字体样式 |

---

## 三、数据模型

### 3.1 config 对象新增字段

```typescript
const config = reactive({
  // 原有字段...
  
  // 新增：视频生成数量
  videoCount: 3,  // 默认3个
  
  // 新增：全局标题配置
  titlePosition: 'top',         // 标题位置
  titleSpacing: 11,              // 主副标题间距(px)
  titleAlignment: 'center',      // 对齐方式
  
  // 新增：主标题配置
  enableMainTitle: true,                  // 启用主标题
  mainTitle: '',                          // 主标题文本
  mainTitleFont: '思源黑体Heavy',          // 主标题字体
  mainTitleLetterSpacing: -50,           // 主标题字间距(px)
  mainTitleSize: 64,                     // 主标题字体大小(px)
  mainTitleColor: '#FFFFFF',             // 主标题颜色
  
  // 新增：副标题配置
  enableSubTitle: false,                 // 启用副标题
  subTitle: '',                          // 副标题文本
  subTitleFont: '思源黑体Heavy',          // 副标题字体
  subTitleLetterSpacing: -50,           // 副标题字间距(px)
  subTitleSize: 64,                     // 副标题字体大小(px)
  subTitleColor: '#FFFFFF'              // 副标题颜色
})
```

---

## 四、UI 交互说明

### 4.1 视频生成数量
- 用户可直接在输入框中输入数字
- 自动限制范围在 1-100 之间
- 输入框右侧显示提示文字"最多100个"

### 4.2 标题配置开关
- **主标题**: 默认开启，显示蓝色标签
- **副标题**: 默认关闭，显示绿色标签
- 开关切换使用 HeadlessUI 的 Switch 组件
- 关闭后隐藏该标题的所有配置项

### 4.3 颜色选择器
- 使用 HTML5 原生 color input
- 颜色选择器宽度固定为 64px
- 右侧配合文本输入框，支持手动输入颜色值
- 两者数据双向绑定

### 4.4 全局配置高亮
- 使用蓝色背景和边框高亮显示
- 包含图标 + 标题的形式
- 与主副标题配置区域视觉区分

---

## 五、样式规范

### 5.1 颜色规范
- **全局配置**: 蓝色主题 (`text-blue-400`, `bg-blue-500/10`, `border-blue-500/30`)
- **主标题**: 蓝色标签 (`text-blue-400`)
- **副标题**: 绿色标签 (`text-green-400`)
- **开关激活**: 蓝色 (`bg-blue-600`)
- **开关未激活**: 灰色 (`bg-gray-700`)

### 5.2 布局规范
- 全局配置使用 3 列网格布局 (`grid-cols-3`)
- 主副标题配置使用 2 列网格布局 (`grid-cols-2`)
- 间距统一使用 `gap-4` (16px)
- 输入框统一使用 `input-primary` 样式类

### 5.3 图标使用
- 全局配置标题图标: `Heading` (16px)
- 主标题标签图标: `Heading` (16px)
- 副标题标签图标: `Heading` (16px)

---

## 六、技术实现

### 6.1 组件导入
新增 `Heading` 图标导入：
```typescript
import { Heading } from 'lucide-vue-next'
```

### 6.2 sheets 配置更新
```typescript
const sheets = [
  { id: 'template', name: '模板选择', icon: Palette },
  { id: 'params', name: '参数配置', icon: Settings },
  { id: 'title', name: '标题配置', icon: Heading },  // 新增
  { id: 'subtitle', name: '字幕配置', icon: Type },
  { id: 'voice', name: '音色配置', icon: Mic }
]
```

### 6.3 表单验证
- 视频生成数量: `min="1"` `max="100"`
- 字体大小: 无限制（依赖用户合理输入）
- 字间距: 支持负数（-50 表示紧密排列）
- 颜色值: 支持十六进制格式 (#RRGGBB)

---

## 七、用户体验优化

### 7.1 默认值设计
- 所有参数都有合理的默认值
- 用户可直接使用默认配置快速生成视频
- 关键参数有明确的单位标识 (px)

### 7.2 视觉反馈
- 开关状态有明确的颜色变化
- 全局配置区域有背景高亮
- 颜色选择器提供实时预览

### 7.3 表单约束
- 数字输入框限制输入类型
- 最大最小值提示
- 单位标识清晰

---

## 八、测试要点

### 8.1 功能测试
- [ ] 视频生成数量输入 1-100 范围内有效
- [ ] 视频生成数量超出范围时的处理
- [ ] 主标题开关切换正常
- [ ] 副标题开关切换正常
- [ ] 颜色选择器与文本输入框数据同步
- [ ] 所有配置参数保存和加载正常

### 8.2 UI 测试
- [ ] 步骤显示为 1/5
- [ ] 标题配置选项卡正常显示
- [ ] 全局配置区域高亮显示正确
- [ ] 主副标题标签颜色区分明确
- [ ] 所有输入框和选择器样式一致

### 8.3 兼容性测试
- [ ] 配置数据与现有模板保存功能兼容
- [ ] 配置数据与视频生成接口对接正常

---

## 九、后续优化建议

### 9.1 功能增强
1. 标题位置"自定义"选项的具体实现（坐标输入）
2. 标题预览功能（实时预览标题效果）
3. 标题动画效果配置
4. 批量生成时的进度显示

### 9.2 交互优化
1. 字体选择支持搜索和预览
2. 颜色选择器增加预设颜色板
3. 字间距、字体大小支持滑块调节
4. 配置模板快速应用

---

**优化完成日期**: 2024-11-18  
**修改文件**: `src/views/WorkspacePage/agents/VideoAgent/VideoConfig.vue`  
**版本**: v2.0.0

