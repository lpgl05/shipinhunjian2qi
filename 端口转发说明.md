# 端口转发访问说明

## 当前服务状态
- **服务地址**: http://localhost:1020
- **服务器IP**: 192.168.0.44
- **端口**: 1020
- **状态**: ✅ 运行中

## 方法一：Cursor 端口转发（推荐）

### 操作步骤：
1. 在 Cursor 底部找到 **"端口"（Ports）** 标签
2. 点击 **"转发端口"（Forward a Port）** 按钮
3. 输入端口号：`1020`
4. 转发成功后，右键点击端口 `1020`
5. 选择 **"在浏览器中打开"（Open in Browser）**

### 快捷键方式：
- 按 `Ctrl+Shift+P`（Mac: `Cmd+Shift+P`）
- 输入 `Forward a Port`
- 选择后输入 `1020`

## 方法二：SSH 端口转发

如果您在本地机器上有 SSH 访问权限，可以使用：

```bash
ssh -L 1020:localhost:1020 用户名@192.168.0.44
```

然后在本地浏览器访问：http://localhost:1020

## 方法三：直接访问（如果在同一网络）

如果您的本地机器和服务器在同一网络内，可以直接访问：
http://192.168.0.44:1020

## 验证服务状态

在服务器上运行以下命令验证：
```bash
curl http://localhost:1020
```

---

**提示**: 如果遇到问题，请检查：
- Cursor 的端口转发功能是否启用
- 防火墙是否允许端口 1020
- 服务是否正常运行

